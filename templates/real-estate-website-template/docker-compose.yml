version: '3.8'

services:
  db:
    image: mariadb:10
    container_name: godrej-mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_DATABASE: u899510431_godrejpune
      MARIADB_USER: u899510431_admin123
      MARIADB_PASSWORD: E;4bwI*lrGX
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: godrej-web-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "mysql://u899510431_admin123:E%3B4bwI%2AlrGX@db:3306/u899510431_godrejpune"
      NODE_ENV: "production"
      AUTH_SECRET: "9ae25c34e022f464010be5349e5d4cb0195e782a2082b2d"
      ADMIN_USER: "admin"
      ADMIN_PASSWORD: "godrej2026"
    depends_on:
      - db
    volumes:
      - uploads_data:/app/public/uploads

volumes:
  mariadb_data:
  uploads_data:
